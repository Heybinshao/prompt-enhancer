# Prompt Enhancer for Hermes

【Hermes 桌面端插件 v1.0.0】输入框「增强提示词」按钮——一句话草稿改写为结构化完整提示词，对标腾讯 WorkBuddy 同款功能。en / 简中 / 繁體。

![prompt-enhancer](assets/cover.jpg)

## 它做什么

输入框右下角（模型选择器左侧）多了一个 ✨ 按钮：

1. 随手打一句模糊的话，例如「帮我写个周报」
2. 点 ✨ → 调用当前会话的模型做一次轻量改写
3. 草稿被替换为结构化提示词：任务/范围/约束/输出形式一次到位
4. 不满意？再点一下（图标变实心）恢复原文

增强结果**只改写、不回答**：不会替你执行任务，只把你的意图整理成 AI 更好执行的提示词。单次无状态调用，不进会话历史，不影响 prompt cache。

## 安装

**方式一：让 Hermes 帮你装（推荐）**

对 Hermes 说：

```
帮我安装这个桌面插件：https://github.com/Heybinshao/prompt-enhancer
```

**方式二：手动放置**

1. 下载本仓库的 `plugin.js`
2. 放到 `~/.hermes/desktop-plugins/prompt-enhancer/plugin.js`（文件夹名必须叫 `prompt-enhancer`）
3. 重启 Hermes 桌面端（⌘Q 后重新打开）

## 使用

| 操作 | 效果 |
|------|------|
| 输入草稿后点 ✨ | 草稿被增强后的提示词替换 |
| 增强后再点 ✨（实心图标） | 恢复原文 |
| 增强等待期继续编辑 | 保护生效：不覆盖你的手改，弹提示 |
| 空输入框点 ✨ | 提示无可增强内容 |

- **模型路由**：继承当前会话的模型——主会话用主会话的，瓦片会话用瓦片自己的（多会话场景精确归属）
- **多行布局**：草稿换行后输入框进入堆叠布局（官方行为），按钮始终紧贴模型选择器左侧
- **语言**：增强结果跟随输入语言（中文进中文出，混排保持混排）

## 边界与说明

- 草稿含引用/路径标签（`@file` 等富引用）时暂不支持增强，会弹提示
- 草稿超过 8000 字符暂不支持
- 增强耗时取决于模型（通常 2-6 秒），转圈即进行中
- ⌘Z 撤销的是你的打字历史，撤销不了增强替换——恢复原文请用按钮
- 需要网关在线（增强走 `llm.oneshot` 网关通道）

## 工作原理

按钮注入官方 `composer.actions` 贡献区；读取草稿（复刻官方 `composerPlainText` 序列化语义）→ 带会话锚点调用 `llm.oneshot` 网关 RPC（无状态、继承会话模型、不污染 prompt cache）→ DOM 写回 + 官方 input 事件镜像进草稿 store。写回前做快照比对，草稿在等待期被手改则放弃应用——绝不覆盖用户的字。

## 版本历史

- **v1.0.0**（2026-09-10）：首发。三语、多会话锚点路由、快照护栏、堆叠布局适配、恢复原文。

## License

MIT
